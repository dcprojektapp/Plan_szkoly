import 'package:flutter/material.dart';

/// Stałe kolorów aplikacji
class AppColors {
  AppColors._();

  // Kolory główne
  static const Color primary = Color(0xFF2196F3);
  static const Color primaryDark = Color(0xFF1976D2);
  static const Color primaryLight = Color(0xFFBBDEFB);
  static const Color accent = Color(0xFF03A9F4);

  // Kolory tła
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFE3F2FD);

  // Kolory tekstu
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Kolory nawigacji
  static const Color routeLine = Color(0xFF4CAF50);
  static const Color routeLineSecondary = Color(0xFF81C784);
  static const Color startPoint = Color(0xFF2196F3);
  static const Color endPoint = Color(0xFFF44336);
  static const Color stairs = Color(0xFFFFEB3B);
  static const Color elevator = Color(0xFF9C27B0);

  // Kolory statusu
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFF9800);
  static const Color error = Color(0xFFF44336);
}
